extends layout

block content
  h1 Registrera spel
  hr

  form(method='POST' action='/registergame')
    input(type='hidden' name='_csrf', value=_csrf)
    div.form-group
      label(for='name') Spelnamn:
      input#name.form-control(type="text", name="name", placeholder="Spelnamn")

    div.form-group
      input(type="checkbox", name="setBased")
      label(for='name') Setbaserat:

    div.form-group
      input(type="checkbox", name="teamBased")
      label(for='name') Lagbaserat:

    div.form-group
      input(type="checkbox", name="reverseCounting")
      label(for='name') Inverterad räkning:

    button.btn.btn-success(type='submit') Registrera
